<%= form_for(@submitted_game) do |f| %>
  <% if @submitted_game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@submitted_game.errors.count, "error") %> prohibited this submitted_game from being saved:</h2>

      <ul>
      <% @submitted_game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= @submitted_game.user ? @submitted_game.user.username : "None" %>
  </div>
  <div class="field">
    <%= f.label :words %><br>
    <%= @submitted_game.words %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.text_field :status %>
  </div>
  <div class="field">
    <%= f.label :reason %><br>
    <%= f.text_field :reason %>
  </div>
  <%= render 'user_auth' %>  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
